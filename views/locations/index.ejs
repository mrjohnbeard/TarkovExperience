<%- include('../partials/header') %>

  <table id="list">
    <thead>
      <tr>
        <th>Area</th>
        <th>Duration</th>
        <th>Difficulty</th>
        <th>Boss</th>
        <th>Players</th>

      </tr>
    </thead>
    <tbody>
      <% locations.forEach(function(l) { %>
        <tr>
          <td>
            <%= l.area %>
          </td>

          <td>
            <%= l.duration %>
          </td>

          <td>
            <%= l.difficulty %>
          </td>
          <td>
            <%= l.boss %>
          </td>
          <td>
            <%= l.players %>
          </td>


        </tr>
        <% }); %>
    </tbody>
  </table>



  <%- include('../partials/footer') %>