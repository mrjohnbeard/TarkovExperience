<%- include('../partials/header') %>

<table id="list">
    <thead>
      <tr>
        <th>Area</th>

      </tr>
    </thead>
    <tbody>
      <% locations.forEach(function(l) { %>
        <tr>
          <td>
            <%= l.area %>
          </td>
        </tr>
        <% }); %>
    </tbody>
  </table>



<%- include('../partials/footer') %>